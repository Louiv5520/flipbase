services:
  - type: web
    name: flipbase-static
    env: static
    buildCommand: cd frontend && npm run build
    staticPublishPath: frontend/build
    envVars:
      - key: NODE_ENV
        value: production
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
